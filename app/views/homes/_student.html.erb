<%= form_tag root_path , method: :get do %>
	<p>
		<%= label_tag :Search %>
		<%= text_field_tag :q , params[:q]%>
		<%= submit_tag "Search"%>
	</p>
<%end%>


<table class= "table table-striped ">
	<tr>
		<th>Username</th>
		<td></td>
		
	</tr>
	
<% @users.each do |a| %>

	<tr>
		<td><%= a.username %></td>
		<td><%= button_to 'Create_Event' , home_path(a.id), method: :get %></td>
		<td><%= button_to 'Fee Amount' ,   new_product_path(a.id), method: :get %></td>

	</tr>	
	<% end %>

<!DOCTYPE html>
<html lang="en">
<head>
    
    <style>
        table {
            width: 200px;
            border-collapse: collapse;
        }
       /* table, th, td {
            border: 1px solid black;
        }
        th, td {
            padding: 10px;
        }*/
    </style>
</head>
<table>
	<tr>
		<td>
	<div class="calendar" id="calendar">
		

		<%#= button_to "show Task", event_path, method: :get %>
 </div></td></tr></table>