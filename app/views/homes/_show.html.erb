<%#= button_to "Pay Fees" , products_path, method: :get %>
<br>
    <strong>User : </strong>
    <%= current_user.username %>
    <br>
 <table>
  <tr>
    <th>title</th>
    <th>description</th>
    <th>Start date</th>  
    <th>End date</th> 
   
  </tr>
  <tr>
    <td></td>
    <% @events.each do |event| %>
    <tr>
      <td><%= event.title %></td>
      <td><%= event.description %></td>
      <td><%= event.start_date %></td>
      <td><%= event.end_date %></td>
    </tr>
    <!-- <td><%#= button_to "Pay Fees" , products_path, method: :get %></td> -->
    <% end %>
  </tr>
</div>
</table>
    

